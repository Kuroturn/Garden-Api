package com.garden.api.core.object;

/**
 * State enum to define whether an {@link com.garden.api.core.animation.AnimationController} should continue or stop
 */
public enum PlayState {
	CONTINUE,
	STOP
}
